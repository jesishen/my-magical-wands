/**
 * The canvas lives in the root layout and reads the current route to pick a
 * theme, so each wand page renders nothing itself. The file's existence is
 * what creates the URL.
 */
export default function Page() {
  return null;
}
